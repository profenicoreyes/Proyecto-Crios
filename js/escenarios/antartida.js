/* Datos del escenario 'Antártida' — contenido solamente (fase 1) */
(function(){
  'use strict';

  globalThis.ESCENARIO_ANTARTIDA = Object.freeze({
    id: 'antartida',
    nombre: 'Antártida',
    descripcion: 'Escenario base: meseta antártica recuperada (estructura de datos)',
    version: '1.0',

    ia: Object.freeze({}),

    introduccion: Object.freeze({ titulo: 'Bienvenido a la Antártida', texto: 'Introducción provisional.' }),

    sectores: Object.freeze([]),

    registros: Object.freeze([]),

    recursos: Object.freeze({})
  });
})();
